{% extends ":templates:box-with-footer.html.twig" %}

{% block title %}
    {{ "user.profile.your_profile" | trans }}
{% endblock %}

{% block box_title %}
    {{ "user.profile.your_profile" | trans }}
{% endblock %}


{% block box_body_content %}
    <div class="row">
        <div class="col-md-4 profile-picture">
            <div class="picture">
                {{ profile_picture(user) }}
            </div>
        </div>


        <div class="col-md-8">
            <dl class="dl-horizontal">
                <dt>{{ 'user.last_name' | trans }}</dt>
                <dd>{{ user.lastName }}</dd>

                <dt>{{ 'user.first_name' | trans }}</dt>
                <dd>{{ user.firstName }}</dd>

                <dt>{{ 'user.username' | trans }}</dt>
                <dd>{{ user.username }}</dd>

                <dt>{{ 'user.email' | trans }}</dt>
                <dd>{{ user.email }}</dd>
            </dl>
        </div>
    </div>
{% endblock %}


{% block box_footer %}
    {% if has_feature('CanRequestCardFeature') %}
        <a href="{{ path('user_request_card') }}" class="btn btn-default">{{ 'user.profile.request_card' | trans }}</a>
    {% endif %}

    {% if has_feature('CanChangePasswordFeature') %}
        <a href="{{ path('user_profile_change_password') }}"
           class="btn btn-default">{{ 'user.profile.change_password' | trans }}</a>
    {% endif %}

    <a href="{{ path('user_profile_edit') }}"
       class="btn btn-info pull-right">{{ 'user.profile.edit' | trans }}</a>
{% endblock %}
