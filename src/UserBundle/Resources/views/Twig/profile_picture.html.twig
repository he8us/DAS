{% if user.profilePicture %}
    <img src="{{ vich_uploader_asset( user.profilePicture, 'profilePictureFile' ) | imagine_filter('profile_picture') }}"
         class="{{ class }}" alt="{{ 'layout.user.profile.picture' | trans }}">
{% endif %}
