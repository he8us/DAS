{% extends ':templates:box-with-footer.html.twig' %}

{% block box_title %}{{ 'student.unregister.title' | trans }}{% endblock %}
{% block title %}{{ 'student.unregister.title' | trans }}{% endblock %}


{% block box_body_content %}
    {{ 'student.unregister.confim' | trans }}
{% endblock %}


{% block box_footer %}
    <a href="{{ path('student_lesson_unregister', {'id': lesson.id, 'confirm': 'yes'}) }}" class="btn btn-danger pull-right">
        {{ 'student.unregister.confirm.yes' | trans }}
    </a>

    <a href="{{ path('student_lesson_details', {'id': lesson.id}) }}" class="btn btn-primary pull-right">
        {{ 'student.unregister.confirm.no' | trans }}
    </a>
{% endblock %}
