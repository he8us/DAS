{% if is_granted("ROLE_SUPER_ADMIN") %}
    <li class="header">{{ "feedback.menu_header" | trans }}</li>
    <li>
        <a href="{{ path('he8us_category_admin_homepage') }}">
            <i class="fa fa-folder"></i>
            <span>{{ "feedback.categories.list" | trans }}</span>
        </a>
    </li>
    <li>
        <a href="{{ path('he8us_feedback_admin_homepage') }}">
            <i class="fa fa-comment"></i>
            <span>{{ "feedback.feedback.list" | trans }}</span>
        </a>
    </li>

{% endif %}
